# remap prefix
unbind C-b
set-option -g prefix C-a
bind-key C-a send-prefix

# split panes using
bind M-d split-window -h
bind M-r split-window -v
unbind '"'
unbind %

bind r source-file ~/.config/tmux/tmux.conf
